pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Hello from GitHub Jenkinsfile!'
                echo "trigger test"
            }
        }
    }
}
